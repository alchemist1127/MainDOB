/* Empty stub for v16 bring-up.  Originally part of the v15 driver; left
 * as a placeholder so the Makefile's per-file rules still produce a
 * .o for the link step.  Will be repopulated when the full dv protocol
 * implementation is rebuilt on top of the stable bring-up. */
